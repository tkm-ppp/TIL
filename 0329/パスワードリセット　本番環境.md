# 経緯
開発環境ではパスワードリセットのためのメールが送れた。だが、本番環境ではメールが届かずエラーになる

# 目的
- 本番環境ではメールが届き、パスワードリセットが出来る。

# 現状について
### production設定
```
  # メールの設定
  config.action_mailer.raise_delivery_errors = true # メール送信エラーを通知
  config.action_mailer.perform_caching = false
  host = "https://readlink.onrender.com"
  config.action_mailer.default_url_options = { protocol: 'https', host: host }

  # 本番環境用: Gmail SMTP設定
  if Rails.env.production?
    config.action_mailer.delivery_method = :smtp
    config.action_mailer.smtp_settings = {
      address: 'smtp.gmail.com',
      port: 587,
      domain: 'readlink.onrender.com',
      user_name: ENV['MAILER_SENDER'],
      password: ENV['MAILER_PASSWORD'],
      authentication: 'plain',
      enable_starttls_auto: true
    }
  end
```


### ページの検証でのエラー
```
turbo.es2017-esm.js:766 
 POST https://readlink.onrender.com/users/password 500 (Internal Server Error)
fetchWithTurboHeaders	@	turbo.es2017-esm.js:766
perform	@	turbo.es2017-esm.js:897
await in perform		
start	@	turbo.es2017-esm.js:1189
submitForm	@	turbo.es2017-esm.js:4287
formSubmitted	@	turbo.es2017-esm.js:5477
submitBubbled	@	turbo.es2017-esm.js:1470
```
- 500 (Internal Server Error)

=内部サーバーエラーという意味。

 何らかの要因で、WebサーバーやWebアプリケーションサーバーでHTTPのリクエストを実行できない場合に発生するエラー

